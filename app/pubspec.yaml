name: fingerprint_sensor
description: Leituras biométricas integradas a sensor

version: 1.0.0+1
release_date: 23/11/2022

publish_to: 'none'

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # State management
  get: ^4.6.5

  # Infra & Persistance
  flutter_secure_storage: ^6.0.0
  envied: ^0.2.3
  http: ^0.13.5

  # UI
  persistent_bottom_nav_bar: ^4.0.2
  flutter_staggered_animations: ^1.1.1
  shimmer: ^2.0.0

  # Linter
  analyzer: ^4.1.0

  # Connectivity
  connectivity: ^3.0.6

  # Other
  equatable: ^2.0.5
  package_info_plus: ^3.0.2
  yaml: ^3.1.1
  collection: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  # Code generation
  build_runner: ^2.3.2
  envied_generator: ^0.2.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/lottie/
    - pubspec.yaml
